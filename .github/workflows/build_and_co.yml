name: Build, Sign and Conditional Release

on:
  push:
    branches:
      - '**'

permissions:
  contents: write

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Say hi
        run: echo "Workflow läuft 🎉"
